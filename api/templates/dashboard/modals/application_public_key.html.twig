<div class="modal fade" id="modalPublicKey" tabindex="-1" role="dialog" aria-labelledby="modalPublicKey" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-body">

                <!-- Close -->
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>

                <!-- Heading -->
                <h2 class="font-weight-bold text-center mb-1" id="modalPaymentTitle">
                    {{ 'public key:'|trans|capitalize }}
                </h2>

                <div class="row">
                    <div class="col-12">
                        {% if application.publicKey is defined and application.publicKey is not empty %}
                            <p class="text-muted" style="word-wrap: break-word; font-size: .75rem">{{ application.publicKey }}</p>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
